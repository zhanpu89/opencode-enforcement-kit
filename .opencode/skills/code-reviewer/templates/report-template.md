# 代码评审报告模板

> 使用说明：将所有 `{占位符}` 替换为实际内容。输出 `doc/review/{模块名}_代码评审报告{_RN}.md`
> **纯后端模式**：一~六节（无维度0和前端审查） | **全栈模式**：全部节

---

# {模块名} — 代码评审报告

| 属性 | 值 |
|------|---|
| **文档编号** | CR-YYYYMMDD-NNN |
| **版本** | v1.0.0 |
| **状态** | 🟡 草稿 |
| **评审模式** | 全栈模式（后端 + 前端）/ 纯后端模式 |
| **报告日期** | YYYY-MM-DD |
| **最后更新** | YYYY-MM-DD |
| **评审人** | AI Code Reviewer |
| **关联文档** | {详细设计文档路径（DES-YYYYMMDD-NNN）/ 未提供} |

---

## 一、评审概要

| 项目 | 内容 |
|-----|------|
| 后端结论 | ✅ 通过 / ⚠️ 有条件通过 / ❌ 不通过 |
| 前端结论 | ✅ 通过 / ⚠️ 有条件通过 / ❌ 不通过 / 🔘 不适用（纯后端） |
| 契约一致性 | ✅ 一致 / ❌ 存在断裂（P0） / 🔘 不适用（纯后端） |
| **综合结论** | **✅ 通过 / ⚠️ 有条件通过（无P0但多个P1）/ ❌ 不通过** |
| P0 致命问题 | {N} 个（必须修复，阻断测试） |
| P1 高风险问题 | {N} 个（建议修复） |
| P2 改进建议 | {N} 个（可选优化） |
| 是否允许进入测试阶段 | 是 / 否 |

> **结论说明：** {一句话核心原因}

**等级定义：** P0 = 致命（契约断裂/安全漏洞），必须修复、阻断测试 | P1 = 高风险，建议阻断，P1>2 时有条件通过 | P2 = 改进建议，不阻断

---

## 二、评审范围

### 后端

> 路径依 LC-001/LC-010 填写。按目标语言对应结构填写。

| 层次 | 文件路径 | 说明 |
|------|---------|------|
| 接口层（Controller） | `{实际路径}/{XxxController}` | {N} 个接口 |
| 业务层（Service） | `{实际路径}/{XxxService}` | {N} 个方法 |
| 数据层（Mapper/Repository） | `{实际路径}/{XxxMapper}` | {N} 条 SQL |
| 数据模型（Entity/Model） | `{实际路径}/{Xxx}` | {N} 个字段 |
| DTO/Schema | `{实际路径}/...` | Request/Response |

### 前端（全栈模式）

| 层次 | 文件路径 | 说明 |
|------|---------|------|
| 类型定义 | `frontend/src/types/{module}.ts` | {N} 类型 |
| API 层 | `frontend/src/api/{module}.ts` | {N} 接口调用 |
| 页面/Store/路由 | `frontend/src/{views/stores/router}/...` | 组件+状态+路由 |

### 参考文档

| 文档 | 状态 | 影响 |
|------|------|------|
| 编码规范 | ✅ 已加载 / ⚠️ 未找到 | 规范检查 |
| 详细设计 | ✅ 已加载 / ⚠️ 未找到 | 逻辑+契约 |

---

## 三、维度0：前后端契约一致性（全栈模式专属）

> 纯后端模式下标注"不适用"并跳过。

**结论：** ✅ 一致 / ❌ 发现 {N} 处断裂

| 类型 | 字段/接口 | 后端定义 | 前端定义 | 状态 | 等级 |
|------|----------|---------|---------|------|------|
| 类型 | `UserVO.userName` | `String userName` | `username: string` | ❌ 不一致 | **P0** |
| 类型 | `UserVO.userId` | `Long userId` | `userId: number` | ✅ 一致 | - |
| API | 创建用户 | `POST /api/users` | `POST /api/user` | ❌ URL 不一致 | **P0** |
| API | 用户列表 | `GET /api/users` | `GET /api/users` | ✅ 一致 | - |

> **小结：** {总体评价}

---

## 四、后端审查

### 维度1：编码规范 **结论：** ✅/⚠️/❌

| 检查项 | 状态 | 描述 | 等级 |
|--------|------|------|------|
| 命名规范（类/方法/变量/常量） | ✅/⚠️/❌ | {描述} | P0/P1/P2/- |
| 日志规范（占位符/禁止裸异常打印） | ✅/⚠️/❌ | {描述} | - |

### 维度2：业务逻辑一致性 **结论：** ✅/⚠️/❌/🔘 无文档

| 检查项 | 状态 | 描述 | 等级 |
|--------|------|------|------|
| 业务规则与伪代码忠实度 | ✅/⚠️/❌ | {描述} | - |
| 接口契约符合性（URL/方法/参数） | ✅/⚠️/❌ | {描述} | - |

### 维度3：安全漏洞扫描 **结论：** ✅ 无漏洞 / ❌ {N} 个

| 检查项 | 状态 | 等级 |
|--------|------|------|
| SQL 注入防护（禁止`${}`拼接） | ✅/❌ | P0/- |
| 越权访问防护（数据归属验证） | ✅/❌ | P0/- |
| 输入验证（@Valid/文件上传/XSS） | ✅/❌ | P1/- |

### 维度4：性能反模式 **结论：** ✅ 无问题 / ⚠️ {N} 个风险

| 检查项 | 状态 | 等级 |
|--------|------|------|
| N+1 查询（循环内调用Mapper） | ✅/⚠️ | P1/- |
| 大事务/缓存/查询效率 | ✅/⚠️ | P1/- |

### 维度5：可维护性 **结论：** ✅ 良好 / ⚠️ 一般 / ❌ 较差

| 检查项 | 状态 | 等级 |
|--------|------|------|
| 代码复杂度（方法行数/嵌套） | ✅/⚠️ | P2/- |
| 魔法数字/注释/可测试性 | ✅/⚠️ | P2/- |

---

## 五、前端审查（全栈模式专属）

> 纯后端模式下标注"不适用"并跳过。

**前端综合结论：** ✅ 通过 / ⚠️ 部分通过 / ❌ 不通过

| 子维度 | 检查项 | 状态 | 等级 |
|--------|-------|------|------|
| 6.1 编码规范 | PascalCase、禁止 any、分层规范 | ✅/⚠️/❌ | P1/P2/- |
| 6.2 业务逻辑 | 字段与设计一致、分页参数、loading/error | ✅/⚠️/❌ | P1/- |
| 6.3 安全 | Token 禁止localStorage、XSS（v-html）、路由守卫 | ✅/❌ | P0/P1/- |
| 6.4 性能 | 路由懒加载、watch 无限循环、重复请求 | ✅/⚠️ | P1/P2/- |
| 6.5 可维护性 | 组件复杂度、硬编码 API/枚举 | ✅/⚠️ | P2/- |
| 6.6 框架特化 | Vue3: setup/toRefs/Pinia; React: useEffect/memo/callback | ✅/⚠️/❌ | P1/P2/- |

> **小结：** {总体评价}

---

## 六、综合问题清单

| ID | 栈 | 文件 | 行 | 等级 | 维度 | 问题描述 | 修复建议 | 阻断 |
|----|----|------|---|------|------|---------|---------|------|
| CR-001 | 契约 | `types/user.ts` | 8 | **P0** | 契约 | `username` → `userName` 不一致 | 改为 `userName` | ✅ |
| CR-002 | 后端 | `XxxMapper.xml` | 23 | **P0** | 安全 | `${sortField}` SQL 注入 | 白名单 + `#{}` | ✅ |
| CR-003 | 前端 | `router/index.ts` | 45 | **P1** | 安全 | 无路由权限守卫 | 添加 `meta.requiresAuth` | ⚠️ |
| CR-004 | 后端 | `XxxServiceImpl` | 112 | **P1** | 性能 | N+1 查询 | 改为 `selectBatchIds` | ⚠️ |
| CR-005 | 前端 | `views/user/index.vue` | 67 | **P2** | 规范 | 魔法数字 `status === 1` | 改为枚举常量 | ❌ |

---

## 七、P0 阻断项详情

> 以下问题**必须全部修复**后方可进入测试。按此格式逐一列出：

```
- **位置：** {路径}，第{N}行 | **问题：** {描述} | **风险：** P0 — {原因}
- **修复方案：** 修复前（错误）→ 修复后（正确）代码示例
```

---

## 八、评审结论

### 最终结论

> **{✅ 通过 / ⚠️ 有条件通过 / ❌ 不通过}**。{2-3句话说明依据}

### 问题汇总

| 类型 | 问题ID | 栈 | 问题描述 | 优先级 |
|------|--------|----|---------|--------|
| 阻断项 | CR-001 | 契约 | `types/user.ts` 字段名不一致 | 立即修复 |
| 阻断项 | CR-002 | 后端 | `XxxMapper.xml` SQL 注入 | 立即修复 |
| 建议 | CR-003 | 前端 | 路由无权限守卫 | P1 高 |
| 建议 | CR-004 | 后端 | N+1 查询 | P1 高 |
| 建议 | CR-005 | 前端 | 魔法数字 | P2 低 |

### 下一步行动

1. **修复 P0**（CR-001、CR-002），预计 {N} 小时 | 2. **重审**：修复后提交 `code-reviewer` 复核 | 3. **P1** 本迭代内修复 | 4. **外部测试**：P0 修复通过后移交

---

## 上轮问题修复情况（多轮评审专属）

> 多轮评审时在「下一步行动」前插入本节。首轮无需。

| 上轮ID | 描述 | 修复状态 | 复核结果 | 说明 |
|--------|------|---------|---------|------|
| CR-00N | {描述} | ✅ 已修复 / ⚠️ 部分 / ❌ 未修复 | ✅ 通过 / ⚠️ 仍有问题 / ❌ 未通过 | {说明} |

---

## 版本号变更规则

`vMAJOR.MINOR.PATCH`：**MAJOR** = 结论根本性变化 | **MINOR** = 新增维度 | **PATCH** = 笔误/补充

**多轮评审：** **新建文件**加 `_R2`/`_R3` 后缀，版本重置为 `v1.0.0`。

---

## 变更记录

| 版本 | 日期 | 类型 | 摘要 | 变更人 |
|------|------|------|------|--------|
| v1.0.0 | YYYY-MM-DD | 🆕 新建 | 初始版本 | AI Code Reviewer |
